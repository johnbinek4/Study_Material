#include <>

// a lot are already made, so no need to remake. 
// Data in () is known as argument, and often we need multiple

// ; at end? will depend. 

// may assign, x = funct(x,x,x); then call

// creating fucntions

/*
When to do so? to complete task that is repetitive.
The name of a function is known as an identifier.
*/

// this is the main function, it is called automatically when exceuted. 
// input goes in (), arguments go into parameters

// this is function definition
int multiply(int x, int y){  
    return (x*y); 
}

int x = 5;

multiply(x, 2); // this is the calling

// declaration is done automatically. The code needs to be set above where the code is called. 
